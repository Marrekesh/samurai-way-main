import React from 'react';
import c from './group.module.css'

const Group = () => {
    return (
        <div className={c.groupBlock}>
            group
        </div>
    );
};

export default Group;